<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="/">LOGO</a>
        </div>
        <ul class="nav navbar-nav">
            <li><a href="/">Home</a></li>
            {{#if user}}
                <li><a href="" id="log-off">Log off</a></li>
            {{else}}
                {{#if company}}
                        <li><a href="" id="log-off">Log off</a></li>
                {{else}}
                    <li><a href="/sign-up">Sign up</a></li>
                    <li><a href="/sign-in">Sign in</a></li>
                    <li><a href="/register-company">Register company</a></li>
                {{/if}}
            {{/if}}
        </ul>
        {{#if user}}
            <p class="navbar-text navbar-right">Signed in as <a href="/cabinet" class="navbar-link">{{user.username}}</a></p>
        {{/if}}
        {{#if company}}
            <p class="navbar-text navbar-right">Signed in as <a href="/cabinet-company" class="navbar-link">{{company.companyName}}</a></p>
        {{/if}}
    </div>
</nav>
